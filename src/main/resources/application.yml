spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interviewdb?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghi
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update # JPA自动根据实体类创建、更新数据库表
    show-sql: true # 打印SQL
    properties:
      hibernate:
        format_sql: true # 格式化打印SQL
server:
  port: 8080